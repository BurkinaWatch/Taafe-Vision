/**
 * The mobile artifact intentionally points to Taafé Vision's public website.
 * This keeps the website as the single source of truth for content, routes,
 * contact forms, imagery, and the admin area.
 */
export const SITE_URL = 'https://taafevision.org/';

export const SITE_HOSTS = new Set(['taafevision.org', 'www.taafevision.org']);