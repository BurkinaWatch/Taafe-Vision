[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build && npx drizzle-kit push --force"

[deploy]
startCommand = "node dist/index.cjs"
healthcheckPath = "/api/projects"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
